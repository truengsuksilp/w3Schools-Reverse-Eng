<nav class="navbar" id="navbar">
  
  <!-- Navbar BRAND -->
  <div class="navbar-brand">
    <a class="navbar-item" href="/">
      <i class="fas fa-home"></i>
    </a>
  </div>
  
  <!-- Navbar MENU -->
  <div class="navbar-menu">
      <div class="navbar-start">
        <div class="navbar-item has-dropdown is-hoverable">
          <a class="navbar-link"> 
            Languages 
          </a>
          <div class="navbar-dropdown dropdown-content-background-color">
              <a class="navbar-item dropdown-item-color" href='/exercises/html'> 
                HTML Exercises 
              </a> 
              <a class="navbar-item dropdown-item-color" href='/exercises/css'> 
                CSS Exercises 
              </a>
              <a class="navbar-item dropdown-item-color" href='/exercises/js'> 
                JavaScript Exercises 
              </a>
          </div>
        </div>
      </div>

      <!-- Navbar END -->
      <div class="navbar-end"">
        <% if(typeof user !== 'undefined'){ %>
          <p class="navbar-item" id="account">
            <strong id="account-text">Account:&nbsp</strong>
            <span> <%= user.email %></span>
          </p>
        <% } %>

        <div class="navbar-item">
          <div class="buttons">
            <% for ( i in routes ) { %>
              <a 
                class="button <%= routes[i].class %>" 
                id="<%= routes[i].id %>"
                href="<%= routes[i].href %>"
              > 
                <%= routes[i].buttonTitle %>
              </a>
            <% } %>
          </div>
        </div>
      </div>

  </div>

</nav>